#include <iostream>
#include <vector>
using namespace std;


int main() {
    // vector<vector<int>> grid(10, vector<int>(10, 0));
    vector<vector<int>> grid;

    if (grid.empty()) {
        cout << "empty" << endl;
    } else {
        cout << "something" << endl;
    }
    return 0;
}
